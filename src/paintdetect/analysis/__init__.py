"""Secondary / WIP analysis tooling built on top of the core segmentation models.

Importing these modules is side-effect free (unlike the original scripts, which
ran batch jobs at import time). Note ``pose`` requires OpenCV
(``pip install -e .[analysis]``).
"""
